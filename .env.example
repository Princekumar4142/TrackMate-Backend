PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Not required for the current in-memory store, but included for when
# you swap models/store.js for real Mongoose models:
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/trackmate
# JWT_SECRET=replace-with-a-long-random-string
